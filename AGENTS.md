# AGENTS.md

## 목표와 경로

3D SD 캐릭터의 몸·얼굴·헤어·의상·무기·장비를 생산하고, 조립과 검수를 연결한다.
기본 책임자는 GPT-6 Astra다. 설계 판단, 작업 분해, 결과 통합, 실제 생성 응답·산출물의 근거 판단과 최종 완료 판정은 Astra가 맡는다. 경계가 분명한 구현·테스트·데이터 정리 작업은 GPT-5.6 Sol의 `medium` 추론 강도로 위임할 수 있다.

- 목표 경계는 `backend/`와 `frontend/`다. 백엔드는 작업·파일·Meshy·Blender를, 프론트는 화면과 API 소비를 소유한다.
- Python 구현은 `backend/src/`, 테스트는 `backend/tests/`, 브라우저 앱은 `frontend/src/`에 있다. Python 패키지 이름 `src.*`와 루트 `uv` 명령을 유지한다. `.env`, `uv.lock`, 기본 `data/`는 저장소 루트가 기준이다.
- 새 공장 파츠 이미지 생성은 OpenAI `gpt-image-2.5-sunburst`를 기본으로 사용한다.  
- 코드 수정 요청으로 실제 이미지·Meshy 생성 요청을 대신 실행하지 않는다. 실제 유료 생성은 사용자가 별도로 실행을 지시한 경우에만 한다.
- 공장 원본·이미지·제공자 응답·작업 기록·산출물은 `ASSET_S3_BUCKET`에 저장한다. S3 장애 시 로컬 저장으로 우회하지 않는다. Blender 파일만 실행 중 임시로 내려받고, S3 저장 성공 후 새 임시 파일을 정리한다. 기존 로컬 작업 자료를 임의 삭제하지 않는다.

## 대기·중단·재접속 하네스

- 긴 생성이나 검증을 하나의 동기 도구 호출로 끝까지 기다리지 않는다. 프로세스·로그·작업 ID를 먼저 확보하고 30초 이내의 상태 확인 단위로 진행한다. 60초 안에 확인된 변화와 다음 검증을 짧게 알린다.
- 세션이 끊겨도 이어갈 수 있게 `docs/work-status.md`에 대상 작업 ID, 실행 주소, 완료한 수정·검증, 남은 확인을 기록한다. 재접속하면 Git 상태와 이 기록, 서버 작업 영수증부터 확인한다. 비밀키·제공자 원문은 기록하지 않는다.
- 조회는 제한 시간과 취소 처리를 갖고 마지막 정상 결과를 유지한다. 네트워크 오류·응답 유실·제공자 실패·로컬 조립 실패를 구분한다. 응답 유실은 기존 idempotency key와 입력으로 복구하며 유료 POST를 새로 만들지 않는다.
- 파츠 이미지 수신 → 3D 작업 접수·진행·다운로드 → 리깅·동작 → 조립 결과를 저장된 상태로 연결한다. 일부 실패와 대기는 완료로 표시하지 않으며 완성된 파츠는 보존한다.
- 하네스·복구 수정은 응답 지연/유실, 재접속, 서버 재시작, 중복 제출 방지를 검증한다. fixture 검증과 실제 제공자 응답·산출물 확인을 구분해 보고한다.

## 화면·정리 기준

- 제품 화면에는 입력, 파츠별 상태, 실제 결과, 가능한 작업만 둔다. 홍보 문구와 미구현 기능 안내를 추가하지 않는다.
- UI는 Pretendard와 공통 글래스모피즘 토큰을 사용한다. 임의 슬로건·설명 문구를 덧붙이지 않는다.
- 공장 반복 생산은 버전이 고정된 전후좌우 좌표·미터 단위 크기·공통 몸·시점별 카메라를 사용한다. 정면·측면 이미지는 원본에서 생성하며 두 뷰를 같은 Meshy 작업에 전달한다. 파츠별 임의 bbox 확대는 규격 피팅이 아니다.
- 최신 사용자 지시로 캐릭터 공장의 규격·관통·동작 품질 검사와 관련 테스트를 제거한다. 품질 검사 결과로 이미지 수신·조립·결과 표시를 차단하지 않는다. 공통 좌표·이미지 준비·피팅은 유지하고 완료 여부는 실제 저장된 조립 산출물로 판단한다. 제거한 검사를 통과한 것으로 기록하지 않는다.
- 삭제된 설정·문서·스킬을 자동 복원하지 않는다. 미사용 로직은 진입점·import·테스트·호환 API 참조를 확인한 뒤 정리한다. 사용자 변경과 기존 작업 데이터는 보존한다.
- 실행은 `./start-local.ps1`을 사용한다. 사용자 지시에 따라 pytest·Playwright·실제 생성 검사는 실행하지 않는다. 프론트 `npm run build`와 백엔드 문법·패키지 빌드만 수행한다. 실행 중인 포트의 프로세스를 확인하지 않고 종료하지 않는다.
- 명령하지 않은 쓸데없는 문구는 금지한다.